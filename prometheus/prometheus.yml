global:
  scrape_interval: 60s  #

scrape_configs:
  - job_name: "stock-exporter"
    static_configs:
      - targets: ["stock-exporter:8080"]

alerting:
  alertmanagers:
    - static_configs:
        - targets: ["alertmanager:9093"]

rule_files:
  - "alerts.yml"

